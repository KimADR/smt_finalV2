# Example database URL for Postgres (do NOT commit real secrets)
# DATABASE_URL="postgresql://<user>:<password>@<host>:<port>/<db_name>?schema=public"
DATABASE_URL="postgresql://postgres:root@localhost:5432/smtimp?schema=public"
PORT=4000
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=replace_with_a_secure_value
# Add other environment variables your NestJS app needs below
# EXAMPLE: SHADOW_DATABASE_URL=postgresql://... (do not commit secrets)
